{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["generate", "@dopex-io/ui#build"]
    },
    "build": {
      "outputs": [".next/**"],
      "dependsOn": ["generate", "^build"]
    },
    "storybook": { "cache": false, "persistent": true },
    "lint": {},
    "generate": { "outputs": ["src/graphql/generated"] }
  }
}

/*
 *   This component requires subgraph implementation for
 *   the InsuredLongStrategy contract. All trades performed
 *   by the user will be displayed here.
 */

const TradeHistory = () => {
  return <>You have no trades.</>;
};

export default TradeHistory;

import { AssetPair } from 'pages/straddles/StraddlesChart';

type CandleStickPrice = {
  h: number;
  o: number;
  c: number;
  l: number;
};

export async function getAssetPairPriceData(assetPair: AssetPair): Promise<[]> {
  return [];
}

query getStraddlesUserData($user: ID!) {
  straddles_users(where: { id: $user }) {
    id
    straddlesUserOpenDeposits {
      id
      epoch
      amount
      rollover
    }
    userOpenStraddles {
      id
      epoch
      amount
      strikePrice
      underlyingPurchased
    }
  }
}

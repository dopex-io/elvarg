query getEscrowTransactions {
  orders {
    id
    dealer {
      id
    }
    counterParty {
      id
    }
    quote
    sendAmount
    base
    receiveAmount
    fulfilled
    closed
    transaction {
      timestamp
    }
  }
}
